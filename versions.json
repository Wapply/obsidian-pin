{
  "obsidian": "1.3.5"
}

esbuild.config.mjs
import esbuild from "esbuild";

export default {
  target: "esnext",
  entryPoints: ["main.ts"],
  outdir: "./dist",
  bundle: true,
  minify: true,
  sourcemap: true
};
